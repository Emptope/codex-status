[package]
name = "codex-status"
version = "0.2.1"
edition = "2024"
rust-version = "1.98"
description = "Read-only local session status window"
license = "MIT"
default-run = "codex-status"

[lib]
name = "codex_status"

[features]
default = ["desktop"]
desktop = ["dep:tauri", "dep:tauri-build", "dep:tauri-plugin-notification"]

[build-dependencies]
tauri-build = { version = "2", optional = true , features = [] }

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "io-util", "io-std", "sync", "time", "signal"] }
notify = "8"
walkdir = "2"
tempfile = "3"
toml = "0.9"
tauri = { version = "2", optional = true, features = ["tray-icon"] }
tauri-plugin-notification = { version = "2", optional = true }

[target.'cfg(windows)'.dependencies]
winreg = "0.55"

[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", default-features = false, features = ["std", "NSSound"] }
objc2-foundation = { version = "0.3", default-features = false, features = ["std", "NSString"] }

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"
